# 初心者向けセットアップガイド

このガイドでは、**個人の無料Googleアカウントだけ**を使って、検証用の匿名日記交換システムを準備します。サーバーの契約や有料サービスは不要です。

> [!IMPORTANT]
> 最初は必ず検証用のSpreadsheet・Form・テスト用メールアドレスを使ってください。本番参加者を登録するのは、最後の「動作確認」がすべて終わってからです。

## まず知っておくこと

### 個人の無料アカウントで使えるか

参加者には、投稿時に個人のGoogleアカウントへログインしてもらい、Google Formsで確認済みのメールアドレスを収集します。

Google Formsの「回答を1回に制限」は**オンにしないでください**。オンにすると、毎日投稿するためのFormなのに、一度送信した参加者が翌日以降も投稿できなくなります。

GoogleはFormの公開・共有設定を更新することがあります。画面の文言が少し違う場合も、以下の目的に合う設定を選んでください。

- 参加者はGoogleアカウントにログインして投稿できる。
- 投稿時のメールアドレスを確認済みとして収集する。
- 特定の会社・学校の組織だけに制限しない。
- 回答は1回に制限しない。

Google Formsの共有・回答設定は、公式の[Formの公開と共有方法](https://support.google.com/docs/answer/2839588)も確認してください。確認済みメールアドレスの収集にはGoogleアカウントへのログインが必要です。[Googleの説明](https://support.google.com/appsheet/answer/14915518)も参照してください。

## 用意するもの

- 管理者用のGoogleアカウント 1つ
- 検証用Google Spreadsheet 1つ
- 検証用Google Form 1つ
- テスト用参加者のGoogleアカウント 2つ以上
  - まずは管理者本人の別アカウントや、協力者のアカウントで構いません。
- このリポジトリのファイル一式

## 全体の流れ

1. SpreadsheetとGoogle Formを作る。
2. Formでメールアドレス収集と日記本文の質問を設定する。
3. SpreadsheetにApps Scriptのコードを貼り付ける。
4. 設定値を入力し、管理用シートを自動作成する。
5. テスト参加者を登録し、トリガーを設定する。
6. 2人、4人の順に実際の投稿とメール配信を確認する。

## 1. 検証用Spreadsheetを作る

1. 管理者アカウントで [Google Sheets](https://sheets.new) を開きます。
2. 「空白のスプレッドシート」を作成します。
3. 分かりやすい名前を付けます。例: `匿名日記交換（検証用）`。
4. ブラウザのURLを開いたままにします。URLの `/d/` と次の `/edit` の間にある文字列が **Spreadsheet ID** です。

例:

```text
https://docs.google.com/spreadsheets/d/ここがSpreadsheet_ID/edit
```

Spreadsheetそのものは管理者だけが編集できるようにしてください。参加者へSpreadsheetのURLを共有してはいけません。

## 2. Google Formを作る

1. 管理者アカウントで [Google Forms](https://forms.new) を開きます。
2. 「空白のフォーム」を作成し、例として `匿名日記（検証用）` と名付けます。
3. 質問を1つ追加します。
   - 質問の種類: **段落**
   - 質問タイトル: **`日記本文`**
   - 「必須」をオン
4. Form上部の「設定」を開き、回答に関する設定を行います。
   - 「メールアドレスを収集」をオンにし、**確認済み** を選びます。
   - 会社・学校の組織だけに制限する設定はオフにします。
   - 「回答を1回に制限」はオフのままにします。
   - この最初の動作確認用の版では写真を扱わないため、「ファイルのアップロード」質問は追加しません。
5. Formを公開または共有し、テスト参加者が回答できるようにします。共有先を個別アカウントで指定できる画面では、テスト参加者のGoogleアカウントを追加します。
6. Formの編集画面URLを確認します。URLの `/d/` と次の `/edit` の間にある文字列が **Form ID** です。

> [!WARNING]
> 日記本文の質問タイトルを `日記本文` 以外にした場合、後ほど設定する `DIARY_BODY_ITEM_TITLE` に、そのタイトルを一字一句同じに入力してください。

## 3. Apps Scriptのコードを入れる

### もっとも簡単な方法: ブラウザで貼り付ける

1. 作成したSpreadsheetで「拡張機能」→「Apps Script」を開きます。
2. 最初からある `コード.gs` の内容を消します。
3. このリポジトリの `src/` にある各 `.gs` ファイルと同じ名前のスクリプトファイルを、左側の「＋」→「スクリプト」から作成します。
4. 対応するファイルの内容をコピーして貼り付けます。
5. `src/appsscript.json` の内容は、Apps Scriptの「プロジェクトの設定」で「`appsscript.json` マニフェスト ファイルをエディタで表示する」をオンにしてから、表示されたファイルに貼り付けます。
6. すべて保存します。

### 開発者向け: claspでアップロードする

Node.jsとclaspを使える場合は、`.clasp.json.example` を `.clasp.json` にコピーし、Script IDを設定して `src/` をアップロードできます。`.clasp.json` には実際のIDが入るため、Gitへコミットしません。

## 4. Script Propertiesを設定する

Apps Script画面の左側にある「プロジェクトの設定」を開き、「スクリプト プロパティ」を追加します。次の表のキーと値を、1行ずつ入力してください。

| キー | 入力する値 | 例 |
| --- | --- | --- |
| `SPREADSHEET_ID` | 手順1で確認したSpreadsheet ID | `1Abc...` |
| `ADMIN_EMAILS` | エラー通知を受け取る管理者のメールアドレス。複数ならカンマ区切り | `admin1@example.com,admin2@example.com` |
| `TIMEZONE` | 固定で入力 | `Asia/Tokyo` |
| `MAIL_PROVIDER` | 固定で入力 | `gmail` |
| `DIARY_BODY_ITEM_TITLE` | Formの日記本文の質問タイトル | `日記本文` |
| `FORM_ID` | 手順2で確認したForm ID | `1Xyz...` |
| `WEB_APP_URL` | 手順6でデプロイしたWeb Appの `/exec` URL | `https://script.google.com/macros/s/.../exec` |

`ADMIN_EMAILS`、`SPREADSHEET_ID`、`FORM_ID` は参加者へ見せない管理情報です。Form本文、参加者向けメール、公開ページには書かないでください。

## 5. 管理用シートと参加者を準備する

1. Apps Script上部の関数一覧から `initializeSpreadsheet` を選び、実行します。
2. 初回はGoogleから権限確認が出ます。内容を確認し、管理者アカウントで許可します。
3. Spreadsheetに次のシートができたことを確認します。
   - `Participants`: 参加者の管理用データ
   - `Diaries`: 投稿された日記
   - `Matches`: 日ごとの交換組み合わせ
   - `DeliveryLog`: メール配信の記録
   - `RunLog`: 実行結果の記録
4. Spreadsheetを再読み込みします。「匿名日記システム」というメニューが表示されます。
5. 「匿名日記システム」→「参加者を追加」を選び、テスト参加者のGoogleアカウントのメールアドレスを1人ずつ登録します。

参加者のメールアドレスは管理者が使う内部情報です。`Participants` シートを参加者へ共有しないでください。

## 6. 匿名コメントWeb Appをデプロイする

1. Apps Script右上の「デプロイ」→「新しいデプロイ」を選びます。
2. 種類を「ウェブアプリ」にします。
3. 「次のユーザーとして実行」は管理者本人を選びます。
4. アクセスできるユーザーは、参加者が追加の権限承認をせず利用できる設定を選びます。組織内限定にすると対象外の参加者が使えないため注意してください。
5. デプロイ後に表示される `/exec` で終わるURLを、Script Propertiesの `WEB_APP_URL` に設定します。テスト用の `/dev` URLは設定しません。

Web AppのコードはGoogleアカウントの識別APIを呼び出さず、コメント投稿者のメールアドレスやユーザーIDを保存しません。URLを知る人はコメントできるため、参加者はコメントURLを第三者へ共有しないでください。

## 7. トリガーを設定する

1. Spreadsheetの「匿名日記システム」→「トリガーを設定」を選びます。
2. 初回はGoogleから権限確認が出ます。内容を確認して許可します。
3. 実行後、Apps Script左側の「トリガー」で次の2つを確認します。
   - Form送信時に `onDiaryFormSubmit`
   - 毎日22:05頃に `runDailyExchange`

「トリガーを設定」を再度実行しても、同じ2つのプロジェクトトリガーを作り直すだけです。重複して増えることはありません。

## 8. 管理者が行う最終確認

ここからは、コードのテストではなく、あなたが作成したGoogle Form・Spreadsheet・Gmailが正しくつながっているかを確認する手順です。一度だけ、検証用の参加者で行ってください。

最小構成は、**管理者1人と協力者1人の合計2アカウント**です。管理者自身のGoogleアカウントも `Participants` に登録し、日記を投稿すれば、2人で相互配信を確認できます。なお管理者は管理用Spreadsheetを閲覧できるため、匿名性の確認は協力者が受け取るメールに、書き手のメールアドレス・内部ID・管理URLが含まれないことで行ってください。

1. Apps Scriptで `runMvpSelfTests` を実行し、自己テストに成功したことを示すメッセージが表示されることを確認します。
2. テスト参加者2人が、それぞれFormから日記を投稿します。
3. `Diaries` に2件、`accepted` として保存されたことを確認します。
4. Spreadsheetの「匿名日記システム」→「指定日を実行（検証用）」を選び、`Diaries` の `diary_date` と同じ日付を入力します。
5. 各参加者に、相手の日記だけが届くことを確認します。メールに氏名、メールアドレス、Spreadsheet URL、IDが表示されないことも確認します。
6. 同じ日付でもう一度「指定日を実行」を行います。メールが増えず、`DeliveryLog` も重複しないことを確認します。
7. 本番開始前には、可能であれば4人でも同様に試し、全員が1人ずつ異なる相手の日記を受け取ることを確認します。
8. メールの匿名コメントリンクを開き、コメントを送信します。書き手に届く通知とWeb画面に、コメント投稿者の氏名、メールアドレス、内部ID、管理URLがないことを確認します。

実環境の22:05トリガーを待たずに、検証用メニューから特定日を実行できます。本番ではこのメニューではなく、日次トリガーを使います。

## 開発者向け: ローカルテスト（運用者は不要）

次のコマンドは、ソースコードを変更した開発者だけが実行するローカルテストです。**通常の運用者が実行する必要はありません。** この最初の機能範囲の実装時に実行済みです。

```bash
uv sync --group dev
uv run --group dev python tests/test_gas_source.py
```

このテストは日付境界・マッチング・HTMLの安全な表示などを確認します。Google FormsやGmailとの実際の接続は、上の「管理者が行う最終確認」で確認します。

## 困ったとき

| 状況 | 確認すること |
| --- | --- |
| Formの投稿が `rejected` になる | Formで収集した確認済みメールアドレスと、`Participants` に登録したメールアドレスが同じか確認する。 |
| `日記本文` が見つからないエラー | `DIARY_BODY_ITEM_TITLE` とFormの質問タイトルが完全に一致しているか確認する。 |
| 参加者にFormが開けない | Formの公開・共有設定で、そのGoogleアカウントに回答権限があるか確認する。 |
| メールが届かない | `DeliveryLog` の `status` と `error`、`RunLog`、管理者宛てエラー通知を確認する。 |
| 同じ日付を再実行したい | `DeliveryLog` に既存行がある配信は二重送信を防ぐため自動再送しない。まず送信状況を管理者が確認する。 |
| `DeliveryLog` が `error` | 原因を解消してから「匿名日記システム」→「失敗した配信を再送」で日付を指定する。`error` の行だけが再送される。 |
| `DeliveryLog` が `processing` のまま | Gmailの送信成功後に記録更新だけ失敗した可能性があるため、自動再送しない。送信済みメールとGAS実行ログを管理者が確認する。 |
| `Comments` が `error` | 原因を解消してから「失敗したコメント通知を再送」で日記の日付を指定する。 |
| 不完全な `Matches` が検出された | 配信処理がまだ始まっていないことを確認し、「未配信のマッチを再構築」で日付を指定する。DeliveryLogが1件でもある日は再構築できない。 |

`processing` を確認した後は「processingを確認済みにする」を選び、対象の `delivery_id` を入力します。Gmailで送信済みなら `delivered`、未送信であることを確認できた場合だけ `error` と入力してください。`error` にした行は、その後「失敗した配信を再送」の対象になります。

参加者を `active=false` にすると、その後のマッチング、未実行の配信、再送から除外されます。無効化済み参加者へ過去分を送信することはありません。

## このセットアップ手順に含まれないもの

- 写真の添付
- CSVアーカイブと削除

これらは後続フェーズで追加します。
