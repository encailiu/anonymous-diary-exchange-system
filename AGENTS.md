# AGENTS.md

## プロジェクト概要

Google Forms、Google Sheets、Google Apps Script（GAS）、Google Drive、Gmail だけで運用する、固定参加者向けの匿名日記ランダム交換システムです。対象タイムゾーンは **Asia/Tokyo（JST）** です。

## 最優先の不変条件

実装・レビュー・テストでは、次の順に優先してください。

1. **匿名性**: 参加者に氏名、メールアドレス、各種ID、管理用URL、Googleアカウント情報を表示・送信・ログ出力しない。
2. **誤配信・二重送信防止**: `delivered` と `DeliveryLog` を送信前後で一貫して管理し、再実行しても同じ日記を二重配信しない。
3. **無料運用**: 有料インフラ・有料外部APIを追加しない。Google の標準機能で完結させる。
4. **障害通知**: 例外や送信失敗は全 `ADMIN_EMAILS` 宛てに即時通知し、原因と処理コンテキストを残す。
5. **保守性**: アーカイブ完了を確認してから元データを削除し、指定日以後のデータには触れない。

## 実装規約

- GAS は `docs/4-impl-spec-v1.0.md` のモジュール境界を守る。責務をまたぐ処理を `main.gs` に集約しない。
- 日付・締切判定は常に JST で行う。22:00:00 以降の投稿は翌日分とする。
- 共有リソースを操作する日次処理、再送、アーカイブには `LockService` を使い、finally で解放する。
- 外部に露出するコメントURLには十分に長いランダムな `comment_token` だけを用いる。内部IDやスプレッドシートURLを埋め込まない。
- メール送信は必ず `sendSystemMail(to, subject, body, htmlBody)` を経由する。`GmailApp.sendEmail()` の直接呼び出しを各機能に散在させない。
- 読み手向けのメール、Web画面、例外メッセージに個人情報・内部ID・管理URLが混ざらないことを確認する。
- 設定値・秘密値は `ScriptProperties` または設定モジュールに置く。ソースコードやテストフィクスチャへ実在のメールアドレス、トークン、URLを書かない。

## 日次交換の要件

- 有効な投稿は、参加者マスターに存在し `active = true` のメールアドレスのものだけ。
- 当日投稿者を自己マッチング・重複なしの相互ペアにする。
- 直近7日間の同一組み合わせを可能な限り回避する。
- 奇数人では1人だけスキップし、過去のスキップ回数が多い人と前回スキップ者を優先的に救済する。
- 0人・1人は安全にスキップして記録する。失敗を成功扱いにしない。
- 手動再実行・未送信分再送でも、マッチ・履歴・配信ログが重複しない冪等性を保つ。

## 変更時の確認

変更に応じて、少なくとも以下を確認してください。

- 22:00境界（21:59:59 と 22:00:00）の投稿振り分け
- 偶数・奇数、0人・1人でのマッチング
- 自己マッチ・重複・直近7日間ペアの不許可
- 再実行時の二重配信防止
- コメントURLから送信者・管理情報を逆引きできないこと
- 例外時に複数管理者すべてへ通知されること
- アーカイブ対象だけがCSV化・削除され、対象外データが保持されること

## 参照資料

- 要件: `docs/2-require-spec-v1.0.md`
- 設計: `docs/3-basic-design-v1.0.md`、`docs/4-impl-spec-v1.0.md`
- 実装順・必須テスト: `docs/5-agent-dev-inst.md`

仕様に曖昧さがあり、匿名性・配信対象・データ削除の挙動に影響する場合は、実装前に確認を求めてください。
