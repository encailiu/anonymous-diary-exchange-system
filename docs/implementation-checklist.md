# 実装チェックリスト

このファイルはセッションをまたぐ作業の正本です。項目は、実装と対応する検証が成功した後にだけチェックします。外部環境待ちの項目は未チェックのまま、下部の「次回開始点」に理由を残します。

## Phase 0: 調査と作業基盤

- [x] 仕様書、設計書、開発指示書を確認する。
- [x] GASソース、配備テンプレート、セットアップ手順をリポジトリに追加する。
- [x] 本番用セットアップと非エンジニア向け動作確認手順を分離する。
- [ ] 検証用Google Spreadsheet・Form・テスト参加者を作成する。

## Phase 1: 基盤・設定

- [x] Script PropertiesからSpreadsheet ID、複数管理者、タイムゾーン、メール設定を取得する。
- [x] `Participants`、`Diaries`、`Matches`、`DeliveryLog`、`RunLog` の初期化を実装する。
- [x] `sendSystemMail()` にメール送信を集約する。
- [x] 例外時に全管理者へ通知する実装と、集約メール送信経路を追加する。
- [ ] 検証環境で全管理者への実通知を確認する。
- [ ] 検証環境でシート初期化と複数管理者通知を確認する。

## Phase 2: 日次交換バッチ

- [x] Form送信を正規化し、有効参加者・重複投稿を判定して保存する。
- [x] JST 22:00境界と対象日計算を実装する。
- [x] Lock下で自己・重複なしの相互マッチングを実装する。
- [x] 配信ログで処理中・配信済み・エラーを記録し、自動再送を防止する。
- [x] `error` だけを明示再送し、送信結果不明の `processing` を再送しない運用を実装する。
- [x] 配信失敗・未解決状態を日次処理の成功として記録しない。
- [x] マッチを一括保存し、既存マッチの欠落・重複を再実行時に検出する。
- [x] 配信開始前に限り、不完全なマッチを管理者が安全に再構築できるようにする。
- [x] `processing` をGmail確認後に明示解決できるようにする。
- [x] 無効化済み参加者をマッチング、初回配信、再送から除外する。
- [x] 匿名の日記メールを実装する。
- [x] GAS内で実行する自己テストを追加する。
- [ ] GAS自己テストを検証用Google環境で実行する。
- [x] QuickJSを使うローカルGASロジックテストを実行する（`tests/test_gas_source.py`）。
- [ ] 検証環境で投稿、配信、同日再実行、送信失敗通知を確認する。

## Phase 3: マッチング改善

- [x] 上限付き組み合わせ探索により、直近7日間の同一ペアを可能な限り回避する。
- [x] 奇数時のスキップ履歴と前回スキップ者を考慮する。
- [ ] 検証環境で履歴回避とスキップ救済を確認する。

## 後続フェーズ

- [ ] Phase 4: 写真と匿名コメントWeb Appを実装済み。Google実環境での匿名添付・Web App検証は未完了。
- [ ] Phase 5: CSVアーカイブ、削除を実装済み。Google Drive実環境での権限・出力・削除検証は未完了。
- [ ] Phase 6: ローカル匿名性・状態遷移レビューは完了。Google実環境の権限・障害通知レビューは未完了。

## 検証済みの変更

- `9f34a93`: 初期仕様書、開発規約、uv開発環境を追加。
- `4b52df8`: 配信再送とマッチングの安全性を改善。
- `24e0414`: 配信前の参加資格と復旧操作を強化。
- `3d34c58`: 匿名コメント通知を実装。
- `29e5ae7`: 検証付きCSVアーカイブを実装。
- `e70448e`: 写真を匿名添付で配信。
- `8bf1ed3`: 送信結果不明時の二重送信を防止。

## 次回開始点

検証用Google環境を [SETUP.md](../SETUP.md) で構築し、[VERIFICATION.md](../VERIFICATION.md) を上から実施する。日次配信・再実行・`processing` 解決、匿名コメント、匿名写真添付、複数管理者通知、Driveアーカイブの権限・境界削除を確認する。
