# 非エンジニア向け動作確認手順書・チェックリスト

この文書は、ITエンジニアではない運用者が、検証用Google環境で完成版アプリの動作と匿名性を確認するための手順書です。上から順番に操作し、各チェック欄へ結果を記録してください。

> [!CAUTION]
> 必ず検証用Spreadsheet、Form、Driveフォルダ、テスト用Googleアカウントを使ってください。本番の参加者情報や日記を使わないでください。

## 1. 記録欄

| 項目 | 記入欄 |
| --- | --- |
| 実施日 |  |
| 実施者 |  |
| 検証用Spreadsheet名 |  |
| 検証用Form名 |  |
| テスト参加者数 |  |
| アプリのコミットまたは版 |  |
| 総合結果 | 合格・不合格・未完了 |

この記録にはメールアドレス、トークン、各種ID、管理URLを書かないでください。

## 2. 判定方法

- **合格**: 書かれた期待結果をすべて満たした。
- **不合格**: 期待結果と異なる、または個人情報・管理情報が参加者側に見えた。
- **未実施**: アカウント数や日数が足りず確認していない。

匿名性、誤配信、二重送信、アーカイブ境界のいずれかが不合格なら、本番運用を開始しません。

## 3. 検証環境を準備する

1. [SETUP.md](SETUP.md) の手順1～10を、すべて検証用の名前とアカウントで実施します。
2. テスト用参加者を最低2人、できれば4人登録します。
3. 管理用SpreadsheetとDriveフォルダが「制限付き」であることを確認します。
4. 参加者には回答用Form URLだけを渡します。

チェック：

- [ ] 合格 / [ ] 不合格 — Spreadsheetを閲覧・編集できるのは管理者だけ。
- [ ] 合格 / [ ] 不合格 — アーカイブフォルダは「制限付き」で、共有先は管理者だけ。
- [ ] 合格 / [ ] 不合格 — 参加者へ管理URLやScript Propertiesを渡していない。
- [ ] 合格 / [ ] 不合格 — `Participants`、`Diaries`、`Matches`、`DeliveryLog`、`RunLog`、`AdminNotificationLog`、`Comments` の各シートがある。

## 4. 組み込み自己テストを実行する

1. Spreadsheetを再読み込みします。
2. 「匿名日記システム」→「自己テストを実行」を選びます。
3. `MVP self-tests passed.` と表示されることを確認します。

この自己テストには、21:59:59と22:00:00の境界、0人・1人・偶数・奇数、自己マッチ禁止、重複禁止、直近ペア回避、スキップ公平性、HTMLエスケープ、日付検証が含まれます。

- [ ] 合格 / [ ] 不合格 — 自己テストが最後まで成功した。

## 5. 管理者通知を確認する

1. 「匿名日記システム」→「管理者通知をテスト」を選びます。
2. `ADMIN_EMAILS` に登録した管理者全員の受信箱を確認します。
3. 件名が緊急アラートで、本文に「管理者通知テスト」「動作確認用」とあることを確認します。

- [ ] 合格 / [ ] 不合格 — 全管理者が通知を受信した。
- [ ] 合格 / [ ] 不合格 — 通知を受けなかった管理者が1人もいない。
- [ ] 合格 / [ ] 不合格 — 同じ管理者アドレスを大小文字違いで重複設定しても通知は1通だけになる。

## 6. 2人で本文と写真を投稿する

1. テスト参加者AとBが、それぞれ自分のGoogleアカウントでFormを開きます。
2. Aは識別しやすいテスト本文と、個人情報を含まないJPEG・PNG・GIFを1枚投稿します。
3. Bは別のテスト本文を写真なしで投稿します。
4. 管理者は `Diaries` に2行追加され、両方の `status` が `accepted` であることを確認します。
5. `photo_file_ids` や `comment_token` は管理者シートだけで確認し、参加者へ見せません。

- [ ] 合格 / [ ] 不合格 — 2件とも `accepted`。
- [ ] 合格 / [ ] 不合格 — `diary_date` がJSTの締切規則に沿っている。
- [ ] 合格 / [ ] 不合格 — Formや完了画面に他参加者の情報がない。

## 7. 指定日の交換を実行する

1. `Diaries.diary_date` の日付を確認します。
2. 「匿名日記システム」→「指定日を実行（検証用）」を選びます。
3. 確認した日付を `YYYY-MM-DD` で入力します。
4. AとBの受信箱を確認します。
5. 管理者は `Matches` と `DeliveryLog` を確認します。

期待結果：

- AにはBの日記、BにはAの日記だけが届く。
- 送信者の氏名、メールアドレス、参加者ID、日記ID、Googleアカウント情報、Spreadsheet・Drive管理URLがメールにない。
- Aの写真は `anonymous-photo-1.png` のような名前でBへ添付され、元ファイル名やDrive URLがない。
- `DeliveryLog` は2行で、両方 `delivered`。
- `Matches.status` は `delivered`。

チェック：

- [ ] 合格 / [ ] 不合格 — AとBが正しい相手の日記だけを受信した。
- [ ] 合格 / [ ] 不合格 — 参加者向けメールに個人情報・内部ID・管理URLがない。
- [ ] 合格 / [ ] 不合格 — 写真の名前と配信方法が匿名化されている。
- [ ] 合格 / [ ] 不合格 — 配信状態がすべて `delivered`。

1項目でも不合格なら、以後のリンクを第三者へ転送せず、運用を停止してください。

## 8. 二重送信防止を確認する

1. 手順7と同じ日付で「指定日を実行（検証用）」をもう一度実行します。
2. AとBの受信箱、`Matches`、`DeliveryLog` を再確認します。

- [ ] 合格 / [ ] 不合格 — メールが追加送信されていない。
- [ ] 合格 / [ ] 不合格 — `Matches` の組み合わせ行が増えていない。
- [ ] 合格 / [ ] 不合格 — `DeliveryLog` が2行のままで重複していない。

管理者が `Matches` または `DeliveryLog` の重複・対応不整合を発見した場合は、その日の処理を再実行せず、配信が停止した状態で原因を調査します。破損行を推測で修正して再送しません。

## 9. 匿名コメントを確認する

1. AまたはBが、受信メールの「匿名コメントを送る」を開きます。
2. テストコメントを入力して送信します。
3. 日記の書き手が通知メールを受信することを確認します。
4. 管理者は `Comments` の状態を確認します。

- [ ] 合格 / [ ] 不合格 — コメント画面のURLに見える識別子は長いランダムトークンだけ。
- [ ] 合格 / [ ] 不合格 — Web画面に氏名、メールアドレス、内部ID、管理URLがない。
- [ ] 合格 / [ ] 不合格 — 書き手への通知にコメント投稿者の情報がない。
- [ ] 合格 / [ ] 不合格 — `Comments.status` が `delivered`。
- [ ] 合格 / [ ] 不合格 — 送信完了後に同じPOSTを再送しても、Comments行と通知メールが増えない。

## 10. 無効参加者を除外できることを確認する

この確認は次の検証日に行います。同じ日に既にマッチを作成している場合、その日付を再利用しません。

1. `Participants` でテスト参加者Aの `active` を `false` にします。
2. AとBがそれぞれFormから新しい日記を投稿します。
3. Aの投稿が `rejected`、Bの投稿が `accepted` であることを確認します。
4. 対象日を指定して交換を実行します。
5. Bが1人だけなので、メールが送信されず `skipped_one_submission` になることを確認します。
6. 確認後、必要ならAの `active` を `true` に戻します。

- [ ] 合格 / [ ] 不合格 / [ ] 未実施 — 無効参加者がマッチ・配信対象にならない。
- [ ] 合格 / [ ] 不合格 / [ ] 未実施 — 有効参加者1人の日が安全にスキップされる。
- [ ] 合格 / [ ] 不合格 / [ ] 未実施 — 無効化した参加者の過去の日記では、コメントフォームを表示・送信できない。

## 11. 奇数・偶数マッチを確認する

実際のSheets・Gmail連携を確認する場合は、マッチ未作成の別日を使います。

### 3人の日

1. 有効なテスト参加者3人が投稿します。
2. 対象日の交換を実行します。
3. 2人が相互配信され、1人だけが `skipped` になることを確認します。

- [ ] 合格 / [ ] 不合格 / [ ] 未実施 — 自己マッチと重複がなく、スキップは1人だけ。

### 4人の日

1. 有効なテスト参加者4人が投稿します。
2. 対象日の交換を実行します。
3. 2組ができ、全員が1通ずつ受信することを確認します。

- [ ] 合格 / [ ] 不合格 / [ ] 未実施 — 4人全員が重複なく1回だけマッチした。
- [ ] 合格 / [ ] 不合格 / [ ] 未実施 — 過去7日以内に別の組み合わせが可能なら、同じペアを避けた。

## 12. `processing` の安全な解決を確認する

この確認では検証用シートを手動編集します。本番環境ではテストしません。

1. 手順7で送信済みを確認した `DeliveryLog` の1行を選び、`status` だけを一時的に `processing` に変更します。
2. Gmailの送信済みメールに対象メールがあることを再確認します。
3. 「processingを確認済みにする」を選び、その行の `delivery_id` を入力します。
4. 結果に `delivered` と入力します。
5. 同じ日付の交換を再実行します。

- [ ] 合格 / [ ] 不合格 — 状態が `delivered` に戻る。
- [ ] 合格 / [ ] 不合格 — メールは再送されない。

コメント通知も同様に、送信済みの `Comments` 1行を検証環境で `processing` にし、「コメント通知のprocessingを確認」から `delivered` へ戻せます。

- [ ] 合格 / [ ] 不合格 / [ ] 未実施 — コメント通知を再送せず安全に解決できた。

## 13. アーカイブ境界を確認する

この確認は、手順6以降の検証データが不要になってから最後に行います。

1. 基準日を決めます。基準日より前と、基準日当日以後の検証行がある状態にします。
2. 「旧データをアーカイブして削除」を選びます。
3. 基準日を入力し、表示された確認文を正確に入力します。
4. Driveのアーカイブ実行フォルダを開きます。
5. `Diaries.csv`、`Matches.csv`、`Comments.csv`、`DeliveryLog.csv`、`manifest.json` があることを確認します。
6. 各CSVの対象件数と `manifest.json` の `rowCount` を照合します。
7. Spreadsheetで、基準日より前の対象行だけが消え、基準日当日とそれ以後が残っていることを確認します。
8. 22:00以降に投稿され翌日分となった行と、不正な `diary_date` の検証行が削除されていないことを確認します。

- [ ] 合格 / [ ] 不合格 — 4つのCSVとマニフェストが作成された。
- [ ] 合格 / [ ] 不合格 — CSVとマニフェストの件数が一致した。
- [ ] 合格 / [ ] 不合格 — 基準日より前だけが削除された。
- [ ] 合格 / [ ] 不合格 — 基準日当日とそれ以後が保持された。
- [ ] 合格 / [ ] 不合格 — `submitted_at` が基準日前でも、`diary_date` が基準日以後の行は保持された。
- [ ] 合格 / [ ] 不合格 — 不正・空欄の `diary_date` を持つ行は保持された。
- [ ] 合格 / [ ] 不合格 — 全管理者が完了通知を受信した。

完了通知の一部が送れなかった場合、処理はエラーとして表示されます。削除自体が完了している可能性があるため、再実行する前に実行フォルダ、マニフェスト、各シートの対象行を確認します。

## 14. トリガーを確認する

1. Apps Script左側の「トリガー」を開きます。
2. `onDiaryFormSubmit` のForm送信トリガーが1件あることを確認します。
3. `runDailyExchange` の毎日22時台のトリガーが1件あることを確認します。
4. 同じハンドラの重複トリガーがないことを確認します。

- [ ] 合格 / [ ] 不合格 — 必要な2トリガーだけが存在する。
- [ ] 合格 / [ ] 不合格 — `runDailyExchange` の実行時刻が22:00より前になっていない。

## 15. 総合判定

### 本番開始に必須

- [ ] 管理リソースが参加者へ非公開
- [ ] 組み込み自己テスト成功
- [ ] 全管理者への通知成功
- [ ] 2人の相互配信成功
- [ ] 二重送信なし
- [ ] 参加者向けメールに個人情報・内部ID・管理URLなし
- [ ] 写真の匿名添付成功
- [ ] 匿名コメントと通知成功
- [ ] 必要なトリガーが重複なく存在

### 運用開始前に推奨

- [ ] 無効参加者と1人日の確認
- [ ] 3人・4人マッチの確認
- [ ] `processing` 解決の確認
- [ ] アーカイブ境界の確認

最終結果：

- [ ] **合格** — 必須項目がすべて合格。本番セットアップへ進める。
- [ ] **不合格** — 1件以上不合格。参加者へ公開せず原因を解消して再確認する。
- [ ] **未完了** — 必須項目に未実施がある。

不合格時は、参加者のメール本文や公開画面へエラー詳細を転載しないでください。管理者だけが `RunLog`、`DeliveryLog`、`Comments`、Apps Script実行ログを確認します。
